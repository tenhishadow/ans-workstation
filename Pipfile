[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
ansible = "*"
yamllint = "*"

[scripts]
install = "ansible-playbook playbook.yml --diff"
pkg     = "ansible-playbook playbook.yml -t install_packages --diff"
