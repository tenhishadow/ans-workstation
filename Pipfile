[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
ansible = "2.9.9"

[scripts]
install = "ansible-playbook playbook.yml --diff"
install_packages = "ansible-playbook playbook.yml -t install_packages"
