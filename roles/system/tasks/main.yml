---
- name: Vars | include for {{ ansible.os_family | lower }}
  ansible.builtin.include_vars: "{{ item }}"
  loop:
    - "{{ ansible.os_family | lower }}.yml"
    - "{{ ansible.os_family | lower }}-packages.yml"

- name: Packages | install
  become: true
  ansible.builtin.package:
    name: "{{ packages }}"
    state: present

- name: Time | configure
  ansible.builtin.include_tasks:
    file: time.yml

- name: OS-common configuration
  ansible.builtin.include_tasks:
    file: system.yml

- name: OS-related configuration
  ansible.builtin.include_tasks:
    file: "{{ ansible.os_family | lower }}-tasks.yml"

- name: docker config
  ansible.builtin.include_tasks:
    file: docker.yml
  when:
    - ansible_facts['virtualization_type'] != 'docker'
    - lookup('env', 'CI') != 'true' # for CirleCI

- name: laptop-related configuration
  ansible.builtin.include_tasks:
    file: laptop.yml
